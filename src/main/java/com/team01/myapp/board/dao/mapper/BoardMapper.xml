<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coderby.myapp.member.dao.mapper.IMemberRepository">
	<select id="selectArticleListByCategory" parameterType="hashmap" resultType="com.team01.myapp.board.model.Board">
		SELECT
			BOARD_ID as "boardId",
			CATEGORY_ID as "categoryId",
			WRITER as "writer",
			EMAIL
	</select>
</mapper>